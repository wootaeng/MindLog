<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mindlog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Mindlog" parent="Base.Theme.Mindlog" />

    <!-- Splash Screen Theme -->
    <style name="Theme.Mindlog.Starting" parent="Theme.SplashScreen">
        <!-- Android 12+ SplashScreen API -->
        <item name="windowSplashScreenBackground">@color/background_cream</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Mindlog</item>

        <!-- Legacy Splash (Android 11 and below) -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/background_cream</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>